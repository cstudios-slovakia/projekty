#!/usr/bin/expect -f
set timeout -1
set host "uid1102122@shell.r5.websupport.sk"
set password "793921881f"
set port "28865"

spawn ssh -o StrictHostKeyChecking=no -p $port $host "cd cstudios.sk/sub/projektstaging/frontend && npm install && npm run build && cd .. && composer update"
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
