#!/usr/bin/expect -f
set timeout 60
set password "ea453634da"
set user "uid1102122"
set host "shell.r5.websupport.sk"
set port "25507"

spawn ssh -p $port -o StrictHostKeyChecking=no $user@$host
expect "password:"
send "$password\r"
expect "$ "

# Run the migration script manually on the staging server
send "cd cstudios.sk/sub/projektyerik && php vendor/cstudios-slovakia/projekty/api/migrate.php\r"
expect "$ "

send "exit\r"
expect eof
