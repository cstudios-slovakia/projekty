#!/usr/bin/expect -f
set password "86f68795a2"
spawn ssh -o StrictHostKeyChecking=no uid1102122@shell.r5.websupport.sk -p26100 "pwd && ls -la && ls -la web"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
