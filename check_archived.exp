#!/usr/bin/expect -f
set timeout 30
set password "5b2d22bd5f"
set user "uid1102122"
set host "shell.r5.websupport.sk"
set port "26799"

spawn ssh -p $port -o StrictHostKeyChecking=no $user@$host "cd cstudios.sk/sub/projektyerik && php -r \"require 'api/db.php'; \\\$stmt = \\\$pdo->query('SELECT is_archived, COUNT(*) as count FROM projects GROUP BY is_archived'); print_r(\\\$stmt->fetchAll(PDO::FETCH_ASSOC));\""
expect "password:"
send "$password\r"
expect eof
